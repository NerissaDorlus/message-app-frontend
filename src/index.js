import React from "react";
import ReactDOM from "react-dom/client";
import Affirmations from "./components/Affirmation";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
  <React.StrictMode>
    <Affirmations />
  </React.StrictMode>
);
